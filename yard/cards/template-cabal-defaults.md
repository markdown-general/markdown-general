template-cabal-defaults ⟜ standard cabal stanzas and boilerplate

**instruction**
provide standard stanzas: file header + metadata, common-properties, library stanza, test stanza, benchmark stanza, default extensions
when creating new package, use these defaults for consistency
