cabal-repl ⟜ load project interactively, test modules without full compile
  * [ ] 
**input** ⟜ repo with .cabal file

**output** ⟜ repl prompt where you can :load, :type, test

**instruction**
cd [repo]
cabal repl

**comment** ⟜ interactive / lightweight / zero state change / exit with :quit
